network:
    localhost: #192.168.20.139 
webservice:
    port:  8080
    conns: 900
etcdmgr:
    path: /tmp/unix_socketRF
chat:
    port: 9090
log:
    type: date #date | size
    maxdays: 7 #for type: date
    maxfiles:  5 #for type: size 
    maxsize: 1048576 #for type: size && the size per file && the unit is byte
    level: 'debug,info,warn,error,critical' #'debug,info,warn,error,critical'
